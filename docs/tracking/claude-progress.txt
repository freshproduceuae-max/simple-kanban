Plan - per-session notes for the next AI.
Terser than progress.md. One line per beat. Reverse-chronological (newest at top).

---

2026-04-21 F07 open — Council shelf scaffold
- PR #25 (F06) merged 4801ef1. F06 passes:true.
- F07 on feat/v0.4-F07-council-shelf. Replaced the four stub files with real impls + a composite:
  * ShelfContainer — <aside> fixed inset-x-0 bottom-0, mx-auto max-w-[1216px] so it spans the board frame at ≥1280px (§6.3) and full-width at 375px (§6.2). bg-surface-shelf + top border border-border-default. No floating chrome (§7.1).
  * ShelfHeader — always-visible strip. Fraunces "Council" label (one voice, §10.1). Hosts the toggle.
  * ShelfToggle — text-led disclosure, aria-expanded + aria-controls. Caret rotates via --motion-duration-fast + --motion-ease-standard. Focus ring via shadow-ring-focus.
  * ShelfBody — role="region", editorial flow (§8.3 no bubble chrome). Scroll-capped 60vh. Collapse/expand uses the `grid-rows-[0fr] ↔ grid-rows-[1fr]` technique with transition-[grid-template-rows] duration-duration-medium ease-ease-standard — that IS the 300ms --motion-ease-standard contract from §9.3 without needing explicit heights.
  * CouncilShelf — composite. useState + useId wires disclosure → region.
- Mounted in app/page.tsx next to <Board>. Board gains pb-space-16 so the collapsed header strip doesn't occlude the last card.
- F06 typecheck regression fixed in passing: layout-fonts.test.tsx used regex /s flag (requires TS target es2018+, our default rejects it). Swapped to [\s\S]*? — same semantics, portable. Slipped through F06 merge.
- Tests +7 → 176/176. components/__tests__/CouncilShelf.test.tsx covers: sticky-bottom surface tokens, disclosure wiring, toggle click, grid-rows motion classes, initialOpen, children render without bubble chrome (walks ancestors up to the region), placeholder line when empty.
- typecheck 0, lint clean, 176/176, build compiles.

---

2026-04-21 F06 open — canonical v0.4 design tokens
- PR #24 (F05) merged 359ea3a. F05 passes:true. Codex re-review clean after the boundary fix.
- F06 on feat/v0.4-F06-design-tokens. Pure token pass, no behaviour change:
  * app/globals.css rewritten — full canonical token set from design-system.md §4 color (13), §5 typography (11), §6 spacing (8), §7 surfaces/elevation (5), §9 motion (5). Font family vars reference --font-fraunces / --font-ibm-plex-sans / --font-jetbrains-mono. html/body now use --color-surface-canvas + --color-ink-900 + --font-family-body.
  * app/layout.tsx rewritten — next/font/google registers Fraunces (display, 400/500/600 + italic), IBM_Plex_Sans (body, 400/500), JetBrains_Mono (mono, 400/500). All three .variable handles applied on <html>. display: 'swap' everywhere.
  * Token pass applied across components/{Board,Column,TaskCard,TaskDialog}.tsx and app/(auth)/sign-in/{page,sign-in-form}.tsx. Surfaces → surface-canvas/shelf/pressed/card. Text → ink-900/700/500. Borders → border-default. Accents → accent-terra (overdue/error), accent-moss (success-adjacent), accent-plum (reserved).
  * Overdue styling moved red-500 → accent-terra-500/700. TaskCard.test.tsx updated to match (border-red-500 → border-accent-terra-500).
  * Modal scrim kept as bg-black/40 (convention, not a brand colour — alpha modifiers don't work on CSS-var-backed tokens).
- Tests +46 → 169/169:
  * app/__tests__/design-tokens.test.ts — 45 it.each over CANONICAL_TOKENS asserting each token is *defined* (regex /<name>\s*:/) in globals.css, plus legacy-name ban (--paper / --ink / --terra must not appear).
  * app/__tests__/layout-fonts.test.tsx — 3 source-level assertions: next/font/google import present, three CSS variable names exposed, all three .variable handles interpolated on <html>. Source read via fs.readFileSync — no next/font runtime needed.
- typecheck 0, lint clean, build compiles, 169/169.

---

2026-04-21 F05 Codex P1 fix — boundary restored
- Codex P1: lib/board/get-task-repository.ts was building the repo from createServerClient inside the board layer. Functionally OK but it reintroduced the F01 coupling and set a bad precedent for product code. Real blocker.
- Moved factory to lib/persistence/server.ts#getTaskRepository(). Deleted lib/board/get-task-repository.ts.
- Extracted getAuthedUserId() to lib/auth/current-user.ts so actions.ts doesn't import from lib/supabase for auth either. lib/board/** now grep-clean of supabase imports (only comments mention it).
- Added F05 regression guard to lib/__tests__/boundary-rule.test.ts: lib/board/** importing @supabase/ssr fails boundaries/external. 5/5.
- typecheck 0, lint clean, 123/123 green, build compiles.

2026-04-21 F05 open — board migration from localStorage to Supabase
- PR #23 (F04) merged 74bee1a. F04 passes:true.
- F05 on feat/v0.4-F05-board-migration. Big feature, shipped as one PR:
  * lib/persistence/supabase-task-repository.ts — real TaskRepository. Filters by user_id on every query, not just RLS. Refuses empty ApprovalContext before hitting the client (gate fails fast). update sets updated_at explicitly.
  * lib/board/approval.ts — mintUserApprovalContext() returns {proposalId: randomUUID, approvalToken: 32 random bytes base64url}. Real verification is F12's; the repository refusing empties catches call-site regressions.
  * lib/board/mappers.ts — taskRowToTask + dueDateToOverdueAt. Kept app-level Task shape untouched (id, title, description, dueDate, status, createdAt) so components/** don't move. position = createdAt (ms) preserves v0.1 insertion order.
  * lib/board/get-task-repository.ts — server-only factory. Binds SupabaseTaskRepository to the current request's server client (cookies → RLS).
  * lib/board/actions.ts — Server Actions: listTasks, create, edit, move, delete, migrateLocalTasks. Each action resolves userId from supabase.auth.getUser(), mints an ApprovalContext, calls the repo, revalidatePath('/'). Uniform ActionResult<T> so client rolls back optimistic updates on {ok:false}.
  * app/page.tsx — now server component. Calls listTasksAction(), shows calm alert on failure.
  * components/Board.tsx — refactored to take initialTasks prop. useTransition + useState for optimistic CRUD. role="alert" banner for errors. "Saving…" for pending. One-time mount effect: if server empty + localStorage has v0.1 tasks, calls migrateLocalTasksAction, clears localStorage, window.location.reload() to re-fetch canonical rows. Three-column layout untouched.
- Removed lib/useTasks.ts + two test files (dead after the server-component pivot).
- vitest.config.ts: added exclude ["**/.claude/**"] — stale worktree copies from the Agent tool's isolation were being picked up (147 count was inflated; real count is 122 post-exclude).
- Tests +20: mappers (6), approval (4), supabase-task-repository builder-chain (6), Board shell + hydration (4).
- typecheck 0, lint clean, 122/122, build compiles.
- F12 TODO: wire proposal-row lookup + token-hash compare into assertApprovalContext. The call-sites already pass a context.

2026-04-21 F04 open — beta allowlist enforcement
- PR #22 (F03) merged 98c0a41 on main. F03 passes:true. Codex re-review clean after ?next= round-trip fix.
- F04 on feat/v0.4-F04-beta-allowlist. Implemented per features.json steps:
  * app/auth/callback/route.ts — after successful exchangeCodeForSession, call supabase.auth.getUser() and run isAllowed(email) against COUNCIL_BETA_ALLOWLIST. On miss: supabase.auth.signOut() (session cookie must not survive the redirect) then NextResponse.redirect(/sign-in?error=not_on_allowlist). Deny-by-default when the env var is unset/empty (readAllowlist returns [], isAllowed returns false).
  * lib/auth/sign-in-error.ts — new signInErrorMessage(code) translator. 'not_on_allowlist' → calm first-person sentence per design-system voice: "This address isn't on the v0.4 beta list yet. If you think it should be, reply to the invite and I will add it." 'missing_code' → helpful request-a-new-one line. Unknown strings pass through unchanged so we never swallow a Supabase provider message.
  * app/(auth)/sign-in/page.tsx — reads searchParams?.error, translates via signInErrorMessage, renders role="alert" red block above the form.
- Tests:
  * lib/auth/__tests__/sign-in-error.test.ts — 4 tests. Null/undefined/empty → null. 'not_on_allowlist' → contains "beta list" + "reply to the invite", and an ASCII-only check via charCodeAt loop (not regex /u flag — tsc target rejects it). 'missing_code' → contains "sign-in link" + "request a new one". Unknown passthrough preserved.
  * app/auth/callback/__tests__/route.test.ts — 8 integration tests via vi.mock('@/lib/supabase/server') with scripted exchangeCodeForSession/getUser/signOut mocks. Covers: missing_code redirect (no exchange), provider error passthrough (signOut NOT called), allowlist miss tears session down (signOut called once), deny-by-default when env unset, happy path → /, ?next=/history honored, open-redirect //evil.com stripped.
- 147/147 pass (136 → 147, +11). typecheck 0, lint clean, build compiles.
- Note: COUNCIL_BETA_ALLOWLIST is case-insensitive comma-separated list; readAllowlist already trims + lowercases. isAllowed returns false on empty list (deny-by-default), which is the property F04 depends on.

2026-04-20 Process lapse — response header dropped mid-session
- CD flagged that the 2-line header from project-planning/response-header-convention.md was missing across the post-compaction batch (Phase 10 close, F01 PR #20 open/merge, F02 PR #21 open — roughly 6 replies).
- Root cause: session resumed from compaction with an instruction to "pick up the last task as if the break never happened," and the header convention was not re-primed from CLAUDE.md on resume. The convention is mandatory per CLAUDE.md ("Response header: every reply to the Creative Director starts with the 2-line header…").
- Correction: header restored from this reply forward. No prior replies will be retroactively edited — the transcript is the record.
- Prevention rule (candidate for CLAUDE.md self-improvement block if it repeats):
  * After any /compact or session-resume, the FIRST action before any tool call or reply body is to re-check CLAUDE.md §Workflow Rules for mandatory response conventions. Treat compaction as a fresh session for protocol purposes, not for state.
- Logging this lapse here rather than in CLAUDE.md pending CD call on whether one miss warrants a new rule or whether the existing rule already suffices.

2026-04-21 F03 Codex P1 fix — ?next= round-trip
- Codex correctly flagged that the middleware-set next was never reaching Supabase. Middleware wrote /sign-in?next=/history; callback could honor next; sendMagicLink ignored it. Fix:
  * lib/auth/callback-url.ts — new pure helper buildEmailRedirectTo(origin, next). Applies safeNext; if sanitized value is '/', emits bare /auth/callback (no `next=` param — matches callback's own fallback semantics). Uses URLSearchParams for encoding.
  * app/(auth)/sign-in/page.tsx — now reads searchParams?.next, sanitizes via safeNext, forwards to SignInForm as a prop. Also surfaces searchParams?.error (callback writes it on failure paths).
  * app/(auth)/sign-in/sign-in-form.tsx — accepts `next` prop, renders as hidden input. Server-side sanitization is the source of truth; client re-sanitization would be theater.
  * app/(auth)/sign-in/actions.ts — sendMagicLink now reads formData.get('next') and feeds it to buildEmailRedirectTo. No direct URL manipulation.
- Defense in depth: next is sanitized three times on the server (page's safeNext, buildEmailRedirectTo's internal safeNext, callback's safeNext on redemption). Any single layer failing still blocks the open redirect.
- Tests added:
  * lib/auth/__tests__/callback-url.test.ts — 4 describe blocks covering default/missing, same-origin preserved, open-redirect stripped, URL-encoded query+fragment.
  * app/(auth)/sign-in/__tests__/sign-in-form.test.tsx — render assertions via @testing-library/react (already installed), hidden input present with default '/' + caller-supplied value. Stubbed actions module with vi.mock since the action is server-only.
- 136/136 total. typecheck 0, lint clean, build compiles /sign-in + /auth/callback.

2026-04-21 F03 open — magic-link auth
- PR #21 (F02) merged e1ae080 on main. F02 passes:true. Codex re-review clean.
- F03 on feat/v0.4-F03-magic-link-auth. Implemented per features.json steps:
  * app/(auth)/sign-in/page.tsx — editorial-quiet page, mobile-first max-w-md
  * app/(auth)/sign-in/sign-in-form.tsx — client component, useTransition for pending state, renders success message after OTP sent
  * app/(auth)/sign-in/actions.ts — Server Action `sendMagicLink`. Builds emailRedirectTo from x-forwarded-host / host + x-forwarded-proto so links work on localhost, preview, and prod without env coupling.
  * app/(auth)/sign-out/actions.ts — `signOut` Server Action + redirect to /sign-in
  * app/auth/callback/route.ts — GET handler; exchangeCodeForSession; error branches redirect to /sign-in?error=...; open-redirect guard via safeNext()
  * lib/supabase/middleware.ts — now route-gates: unauth + non-public → redirect to /sign-in?next=<path>; authed at /sign-in → /
- Three pure helpers extracted for testability:
  * lib/auth/safe-next.ts — open-redirect guard (// protocol-relative, /\ backslash smuggling, http:/https: absolute, javascript:/data: schemes all rejected)
  * lib/auth/email.ts — normalize (trim + lowercase) + shallow regex validation; final gate is Supabase
  * lib/auth/public-paths.ts — isPublicPath: exact-match /sign-in, /auth/callback + prefix-match /_next + favicon. Everything else requires auth.
- 14 new unit tests (3 files) across these helpers. Total 130/130.
- Standing rule followed: no allowlist in this PR — that is F04's scope. The callback is structurally ready to accept F04's rejection branch.
- Next after F03 merge: F04 (beta allowlist enforcement on callback).

2026-04-20 F02 Codex P1 fix — apply-time test caught a real bug
- Codex P1 on PR #21: static SQL-text test is insufficient; F02 requires real apply-time verification. CD approved pglite devDep. Added @electric-sql/pglite ^0.4.4.
- apply.test.ts: real Postgres 16 (WASM via pglite), auth schema shim (auth.users, auth.uid() returning null::uuid, anon + authenticated roles created via do-block because pglite doesn't provision them), applies 001..010 in lex order, asserts: every expected table exists, RLS on all, council_metrics_daily view present WITH (security_invoker=true) via reloptions, every hot-path index from migration 010 present, every RLS-enabled table has at least one policy.
- Real bug surfaced on first apply: `column` is a Postgres reserved keyword; 001_init_tasks.sql failed with syntax error. Renamed tasks.column → tasks.board_column everywhere: SQL migration (column definition, check constraint, index), lib/persistence/types.ts TaskRow, lib/persistence/task-repository.ts create/update signatures (including the patch Pick<>).
- This is exactly the class of defect Codex's blocker was designed to surface. The static-integrity test (22 assertions) still passes — it catches "forgot RLS" / "no policy". The apply-time test (5 assertions) catches syntax, reserved words, FK resolution, policy compilation. Different failure classes, both kept.
- Incidental fix: tsc errors TS2802 on Set iteration in integrity.test.ts. Wrapped with Array.from(). Root cause is no explicit `target` in tsconfig.json (Next default doesn't set one); won't touch tsconfig without wider review.
- 116/116 tests pass (89 + 22 integrity + 5 apply-time). typecheck 0, lint clean.
- pglite notes for future phases: contrib extensions loaded via `new PGlite.create({ extensions: { pgcrypto } })`. Auth shim will likely be shared across F05/F20 test harnesses — candidate for extraction to a helper when the 2nd consumer arrives. Not extracted now (YAGNI).

2026-04-20 F01 GREEN + F02 open
- PR #20 merged as e1025d2 on main. Codex re-review clean after P1 fix: replaced config-shape test with real ESLint enforcement via ESLint.lintText({ filePath }) on four virtual fixtures. Added lib/__tests__/eslint.d.ts shim rather than pulling @types/eslint.
- F02 opens on feat/v0.4-F02-schema-migrations. Body already landed in scaffolding (migrations 001-010). This PR flips F02 passes:true and adds supabase/migrations/__tests__/integrity.test.ts — 22 static guards: contiguous 001..010 numbering + every `create table public.X` followed by `alter table public.X enable row level security` in the same file + every RLS-enabled table has at least one policy. Views + indexes-only migrations naturally not caught (views inherit RLS; 010 creates no tables).
- No local Supabase spin-up (docker) — left for F05/F20 integration when a real DB is necessary. Static guards catch the class of bug that actually costs us (forgot to RLS = cross-user leak) without requiring CI docker.
- 111/111 tests pass (89 + 22 new). typecheck 0, lint clean.

2026-04-20 Phase 10 close (GREEN) + Phase 11 open (F01)
- PR #19 merged as c3bc60c on main. Phase 10 GREEN. Codex re-review clean.
- Two blockers fixed in commit 536ec75: (1) tailwind.config.ts rewritten to canonical --color-*/--font-family-*/--shadow-*/--motion-* names only per design-system.md §11 (removed non-canonical --color-surface/--color-moss/--font-display/--shadow-card/--motion-duration-standard); (2) council_metrics_daily view now declared `with (security_invoker = true)` + `revoke all from public, anon; grant select to authenticated` - prevents cross-user aggregate leak via view-owner RLS bypass.
- Non-blocking dep drift also corrected: package.json floors pinned back to plan values (^2.46.1 / ^4.0.1 / ^3.23.8 / ^1.219.2) and lockfile refreshed.
- Phase 11 open on feat/v0.4-F01-persistence-boundary. F01 body already in scaffolding PR; this PR flips features.json F01 passes:true + updates ledger in docs/releases/v0.4-council/progress.md + session log entries. Optional: add ESLint-programmatic integration test verifying @supabase/* boundary rule fires on a council/ fixture (satisfies F01 step 4 more rigorously than unit-level).
- Next after F01 merge: F02 (migrations apply verified) or F03 (magic-link auth), both unblocked.

2026-04-20 Phase 10 plan close + execution PR open
- PR #18 merged. Execution on feat/phase-10-v04-scaffolding.
- npm installs done verbatim: @supabase/supabase-js, @supabase/ssr, @anthropic-ai/sdk, resend, zod (runtime) + supabase CLI + eslint-plugin-boundaries (dev). Lockfile committed.
- lib/supabase/{browser,server,service,middleware}.ts implemented with @supabase/ssr cookie adapter. lib/persistence/** typed repos (TaskRepository requires ApprovalContext {proposalId, approvalToken} on every mutation - Write Gate enforced at the type level). lib/council/{researcher,consolidator,critic} stubs + shared/{voice,risk,token-budget}.ts with real helpers. lib/auth/beta-allowlist.ts + tests. lib/observability/{failure-class,error-email,instrumentation}.ts.
- app routes: (auth)/sign-in, auth/callback, api/council/{proposals,proposals/[id]/approve,chat,plan,advise,greeting}, history, settings/council, admin/metrics. Root middleware.ts wired.
- components/{council-shelf,thinking-stream,proposal-card} stubs.
- 10 Supabase migrations with owner-only RLS. council_metrics_daily view added for F22 daily cap queries. Indexes on hot paths (session lookup, metrics range, proposals-by-status-expiry).
- .env.example with 9 env vars. tailwind.config.ts extended with --color-*/--font-*/--space-*/--shadow-*/--motion-* namespace (values applied at F06).
- .eslintrc.json: eslint-plugin-boundaries disallows @supabase/* from council/auth/observability/app/components/lib-other; no-restricted-syntax bans NEXT_PUBLIC_(ANTHROPIC|RESEND|SUPABASE_SERVICE|COUNCIL)* env reads. @typescript-eslint/no-unused-vars honors _ prefix.
- Green: typecheck (0 errors), lint (clean), vitest (85 tests pass), next build (15 routes, middleware 79.5kB).
- Zero deviations from the approved Phase 10 plan. Nothing new added. Every [impl] file listed in plan §3 is implemented; every stub file throws with its owning feature ID in the message.
- Next: Codex blocking review on this PR (touches lib/, app/, package*.json — not docs-only). After merge, Phase 11 opens first feature PR (F02 schema apply OR F04 allowlist — F01 body already landed in this scaffolding PR).

2026-04-20 Phase 09 close + Phase 10 plan open
- Phase 09 closed GREEN. PR #17 merged on main. docs/releases/v0.4-council/progress.md is the canonical delivery tracker.
- Phase 10 plan drafted on chore/phase-10-v04-scaffolding-plan. Single PR. Merge = explicit approval for npm installs + scaffold + migrations + CI rules.
- New CD standing rule: "If Phase 10 plan clearly lists packages + reasons + dirs + migrations + CI rules, approval on that plan PR counts as explicit approval for the listed npm installs and scaffolding execution." One decision point only.
- Runtime deps: @supabase/supabase-js ^2.46.1, @supabase/ssr ^0.5.2, @anthropic-ai/sdk ^0.39.0, resend ^4.0.1, zod ^3.23.8. Dev deps: supabase CLI ^1.219.2, eslint-plugin-boundaries ^4.2.2. 10 Supabase migrations. 6 CI rules incl. @supabase/* import-boundary enforcement + NEXT_PUBLIC_ AI-env ban.
- Execution PR follows plan-PR merge. Phase 11 (feature PRs) starts after scaffolding merges.

2026-04-20 Phase 08 close + Phase 09 open
- Phase 08 closed GREEN. PR #16 merged as d1bd04a on main. features.json (32 features: 22 alpha / 4 beta / 6 final) + features-README.md + phase-08 tracking doc on main.
- Pre-merge scope-fidelity audit confirmed: PRD §§3-16 all covered, zero v0.5/v0.6/v1.0 creep, all 5 [CD PICK] defaults preserved (F03 magic link, F04 allowlist, F21/F26 Vercel Analytics + /admin/metrics, F22 500k cap, F29 forever+purge).
- Phase 09 opened on chore/phase-09-v04-progress-tracking. Deliverable: docs/releases/v0.4-council/progress.md - feature ledger table (mirrors features.json passes field), milestone gates, acceptance proxies from PRD §3, risks, session log.
- CD standing instruction 2026-04-20: "bypass all information only replies and proceed and give me info all together only" - batch progress into consolidated reports, no interim informational check-ins.
- Next: Phase 10 Scaffolding. First explicit npm-install approval gate per global CLAUDE.md. Feature-level PR cadence begins there.

2026-04-20 Phase 07 close + Phase 08 open
- Phase 07 closed GREEN. PR #15 merged as 2d97029 on main. Canonical v0.4 PRD at docs/releases/v0.4-council/prd.md (19 sections). All 5 [CD PICK] defaults stood: magic link, allowlist beta, forever retention + user purge at final, Vercel Analytics + /admin/metrics, 500k tokens/day cap.
- CD review ran as one-pass product-direction approval. CD instruction: "if you need further review only, i suggest to make it more simpler read" - keep PR reports short.
- Added second self-improvement rule to CLAUDE.md: "Begin every process by inferring, not asking." Retires decision-gate inflation.
- Phase 08 opened on chore/phase-08-v04-feature-list. features.json drafted: 32 features, 22 alpha / 4 beta / 6 final. features-README.md explains fields + Phase 10/11 consumption contract. No [CD PICK] items - feature list is mechanical translation of PRD.
- PR #16 to open next with these four files: features.json, features-README.md, phase-08-v04-feature-list-progress.md, updated progress.md + claude-progress.txt.

2026-04-20 Phase 06 close + Phase 07 open
- Phase 06 closed GREEN. PR #13 merged as cd8ea71 on main. docs/design-system/design-system.md is canonical for v0.4.
- Gemini raw output frozen at docs/design-system/gemini-raw-output.md. Editorial-planner inspiration frozen at docs/design-system/inspiration/editorial-planner-direction.md.
- Retired the "one-decision-at-a-time interview" anti-pattern. PR #14 closed unmerged. CLAUDE.md gained a self-improvement rule: infer from canonical docs, propose defaults with citations, flag genuine-taste items as [CD PICK] inside the draft, PR is the approval surface.
- Phase 07 v0.4 PRD now drafted on chore/phase-07-v04-prd as ONE document (docs/releases/v0.4-council/prd.md), reviewed as ONE PR, merged when redlines settle. Target 3-5 exchanges total.
- Three authoritative inputs: docs/prd/vision.md + docs/design-system/design-system.md + docs/tracking/phase-07-prd-input-queue.md.
- CD-only picks surfaced upfront (see phase-07-v04-prd-progress.md §3): auth shape, invite flow for beta, dashboard location, session-history retention.
- Still no npm installs. That is Phase 10. Still no code. That is Phase 11.
- PR #2 + PR #8 still sidelined, non-blocking.

2026-04-20 overnight
- Phase 05 closed GREEN. PR #11 merged as c052388. Codex two-pass audit: YELLOW -> GREEN.
- Phase 06 Brand Identity prep opened on chore/phase-06-brand-identity-queue.
- Gemini + Codex prompts queued in docs/tracking/phase-06-brand-identity.md sections 4 and 5.
- PR #2 feat/design-refresh surfaced as Phase 06 precondition. Recommended: Option A (close unmerged, extract aesthetic as inspiration).
- Phase 07 v0.4 PRD input queue written at docs/tracking/phase-07-prd-input-queue.md - 10 Codex gaps + 6 Claude-identified gaps + scope-hygiene list.
- Phase 09 tracking stubs scaffolded (this file + progress.md).
- On CD resume: (1) decide PR #2 (A/B/C), (2) paste Gemini prompt, (3) save raw output, (4) paste Codex normalization prompt, (5) close Phase 06, (6) start Phase 07 PRD against the input queue.
- DO NOT install npm packages yet. That is Phase 10 Scaffolding territory and needs explicit CD approval per global CLAUDE.md.
- DO NOT merge PR #2 as-is - it carries v0.6 scope (priority/tags/search/filter) into a v0.1.0 PR, which violates the serial-release lock.

2026-04-20 earlier
- Phases 02-04 landed. Vision + operating-model + project CLAUDE.md on main.
- Phase 05 Codex Chief Architect audit run. YELLOW verdict with 4 blockers + 4 minor + 2 P2 follow-up items.
- Fix PR #11 landed all 10 items across two commits; re-audit returned GREEN.

Earlier project context:
- v0.1.0 shipped on main (pre-methodology). Almost no brand commitment on main currently (Arial body font, two CSS vars).
- Hybrid operating model: Claude Code primary, Codex blocking reviewer.
- Agent split: P1 (by domain) for v0.4; P2 (by lifecycle) for v0.5+.
- Repository boundary: lib/persistence/** for app data, lib/supabase/** for auth/session. Nothing else imports @supabase/*.
- Council Write Gate: every board mutation requires proposalId + server-issued approval token. CI enforces at Phase 10.
