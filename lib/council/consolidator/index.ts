/**
 * Consolidator agent — F10.
 * The user-facing voice. Streaming output. Fail-hard (retry once, then surface).
 *
 * Implementation lands with F10 at Phase 11.
 */
export async function consolidate(): Promise<never> {
  throw new Error('Consolidator: implementation lands with F10');
}
